# srt-error

Shared protocol error types for SRT.

This crate defines the standard error surface used by the `no_std` SRT protocol crates. It is intentionally small, copyable, allocation-free, and does not depend on `std` or external error crates.

Current status: boundary-only scaffold.
