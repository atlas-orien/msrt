# srt-core

Core protocol primitives for SRT.

This crate owns frame and packet type markers, flags, stream identifiers, sequence identifiers, and shared error definitions for the common SRT protocol standard. It is `no_std` and avoids allocation by default.

Current status: boundary-only scaffold.
